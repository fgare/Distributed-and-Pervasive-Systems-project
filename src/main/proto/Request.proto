syntax = "proto3";
package ThermalPowerPlantPackage;

message Request {

  enum Status {
    ELECTION = 0;
    ELECTED = 1;
  }

  int32 id = 1;
  float offeredPrice = 2;
  Status status = 3;

}

service ElectionService {
  rpc handleElection(Request) returns (Request);
}
